import { BlogItemPostedPipe } from './blog-item-posted.pipe';

describe('BlogItemPostedPipe', () => {
  it('create an instance', () => {
    const pipe = new BlogItemPostedPipe();
    expect(pipe).toBeTruthy();
  });
});
